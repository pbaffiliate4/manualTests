var pubsub = require("pubsub");
// Check if the current user has permission to publish messages to the channel called "myChannel"
    // publish message to "myChannel"
    pubsub.publish("fafa", "Hello Device!");
